<%- include('../partials/__head.ejs') %>
<%- include('../partials/__navbar.ejs') %>
  <body>

    <div class="container">
        <% if (messages.success) { %>
          <div class="alert alert-info" role="alert">
           <%- messages.success %>
          </div>
        <% } %>
        <h1 class="text-center">Data Dpi</h1>
        <div class="row">
            <div class="col">
              <a href="/dpi/create" class="btn btn-sm btn-success">Tambah</a>
                <table class="table">
                    <thead>
                      <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nama Dpi</th>
                        <th scope="col">Luas Daerah</th>
                        <th colspan="2" scope="col">Aksi</th>
                      </tr>
                    </thead>
                    <tbody>
                        <% for(var i = 0; i < data.length; i++){ %>
                            <tr>
                                <th scope="row"><%= (i+1) %></th>
                                <td><%= data[i].nama_dpi %></td>
                                <td><%= data[i].luas %></td>
                                <td>
                                    <a href="/dpi/edit/<%= data[i].id_dpi %>" class="btn btn-sm btn-warning">Edit</a>
                                    <a href="/dpi/delete/<%= data[i].id_dpi %>" class="btn btn-sm btn-danger">Delete</a></td>
                            </tr>
                        <% } %>
                    </tbody>
                  </table>
            </div>
            
        </div>

    </div>
  </body>
  <%- include('../partials/__foot.ejs') %>